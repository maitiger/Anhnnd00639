<Page x:ConnectionId='1'
    x:Class="CodeYami.Menu.MusicAnime"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeYami.Menu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:CodeYami.Entity"

    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Pivot>
        <Pivot.Title>
            <TextBlock Text="Anime Music "/>
        </Pivot.Title>
        <Pivot.RightHeader>
            <CommandBar>
                <AppBarButton>
                    <SymbolIcon x:ConnectionId='5' x:Name="Return" Symbol="Refresh"/>
                </AppBarButton>
                <AppBarButton>
                    <SymbolIcon x:ConnectionId='4' x:Name="Play" Symbol="Play"/>
                </AppBarButton>
                <AppBarButton>
                    <SymbolIcon x:ConnectionId='3' x:Name="Pause" Symbol="Pause"/>
                </AppBarButton>
                <AppBarButton>
                    <SymbolIcon x:ConnectionId='2' x:Name="Next" Symbol="Next"/>
                </AppBarButton>


            </CommandBar>
        </Pivot.RightHeader>
        <PivotItem Header="Anime Music">
            <Grid>
                <ListView x:ConnectionId='14'                                 >
                    <ListView.ItemTemplate >
                        <DataTemplate                         >
                            <StackPanel x:ConnectionId='15' Orientation="Horizontal">
                                <Image Source="{Binding thumbnail }" Width="150"/>
                                <StackPanel Orientation="Vertical" >
                                    <TextBlock x:ConnectionId='16'                      />
                                    <TextBlock x:ConnectionId='17'                        />
                                    <TextBlock x:ConnectionId='18'                        />
                                    <TextBlock x:ConnectionId='19' x:Name="txtFilePath" Width="400"
                                     FontSize="20"
                                                              
                                                         />     

                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

        </PivotItem>
       

        <PivotItem Header=" ADD and Show ">
            <StackPanel>
                <TextBox x:ConnectionId='6' x:Name="Name1" Header="Song Name"/>
                <TextBox x:ConnectionId='7' x:Name="Thumnail" Header="Thumnail "/>
                <TextBox x:ConnectionId='8' x:Name="Singer" Header="Singer Name"/>
                <TextBox x:ConnectionId='9' x:Name="Author" Header="Author Name"/>
                <TextBox x:ConnectionId='10' x:Name="LinkSong" Header="LinkSong Name"/>
                <Button x:ConnectionId='11' Content="Add this song"                 />

                <ListView x:ConnectionId='12' Name="Output">
                    <ListView.ItemTemplate>
                        <DataTemplate                         >
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding }" Width="150"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding}" />
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </StackPanel>

        </PivotItem>

    </Pivot>

</Page>

